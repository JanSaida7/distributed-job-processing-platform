[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --cov=app --cov-report=term-missing --cov-fail-under=60"

[tool.coverage.run]
branch = true
source = ["app"]

[tool.ruff]
target-version = "py313"
exclude = ["tests"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
